package main

import "fmt"

func main() {
	input := []string{"bella", "label", "roller"}
	fmt.Println(commonChars(input))
}

func commonChars(A []string) []string {
	if len(A) == 0 {
		return A
	}
	result := []string{}

	for _, str := range A {
		for _, b := range str {

		}

	}

}
